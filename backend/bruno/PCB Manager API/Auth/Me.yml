info:
  name: Me
  type: http
  seq: 6

http:
  method: GET
  url: "{{baseUrl}}/auth/me"
  headers:
    - name: Authorization
      value: Bearer {{accessToken}}
  params:
    - name: ""
      value: ""
      type: query
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
