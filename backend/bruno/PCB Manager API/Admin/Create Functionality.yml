info:
  name: Create Functionality
  type: http
  seq: 6

http:
  method: POST
  url: "{{baseUrl}}/admin/functionalities"
  headers:
    - name: Authorization
      value: Bearer {{accessToken}}
  body:
    type: json
    data: |2-
        {
          "name": "DEF",
          "description": "ABCD"
        }
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
