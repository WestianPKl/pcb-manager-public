info:
  name: Get All Projects Search
  type: http
  seq: 2

http:
  method: POST
  url: "{{baseUrl}}/projects/search"
  headers:
    - name: Authorization
      value: Bearer {{accessToken}}
  body:
    type: json
    data: |-
      {
        "name": "Project 1"
      }
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
