info:
  name: Create Package
  type: http
  seq: 12

http:
  method: POST
  url: "{{baseUrl}}/inventory/packages"
  headers:
    - name: Authorization
      value: Bearer {{accessToken}}
  body:
    type: json
    data: '{ "name": "SOT-23" }'
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
