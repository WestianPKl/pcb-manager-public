info:
  name: Get All Surface Mounts
  type: http
  seq: 15

http:
  method: GET
  url: "{{baseUrl}}/inventory/surface-mounts"
  headers:
    - name: Authorization
      value: Bearer {{accessToken}}
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
