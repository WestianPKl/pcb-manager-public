info:
  name: Get All Packages
  type: http
  seq: 11

http:
  method: GET
  url: "{{baseUrl}}/inventory/packages"
  headers:
    - name: Authorization
      value: Bearer {{accessToken}}
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
