info:
  name: Get Stock Movement
  type: http
  seq: 26

http:
  method: POST
  url: "{{baseUrl}}/inventory/movements/search"
  headers:
    - name: Authorization
      value: Bearer {{accessToken}}
  body:
    type: json
    data: |-
      {
        "name": "Tranzystor BC-385"
      }
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
